# Required Libraries to run the *Zpiciest* work space on planet earth
#### Incase of not having "pip3" use "pip" for the following commands  
#### `pip3 install adafruit-circuitpython-pca9685`
#### `pip install zope.interface`

